import './Header.css';
import React from 'react';

const Header = () => {
    return (
        <div className="header-container">
            <h1>Game<span>Shop.</span></h1>
        </div>
    );
};

export default Header;